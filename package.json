{
  "name": "gen-ai-product-iq",
  "private": true,
  "scripts": {
    "backend": "cd backend && docker-compose up --build",
    "frontend": "cd frontend && npm run dev",
    "dev:all": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}